import styled from 'styled-components';

export const Container = styled.div`
  .topbar {
    background-color: #fff;
    overflow: hidden;
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 9;
  }
  .hidden {
    visibility: hidden;
  }
`;
